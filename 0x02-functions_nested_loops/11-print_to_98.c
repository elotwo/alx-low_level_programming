#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
 */
void print_to_98(int n)
{
	int i =  0;
	while (i <= n)
	{
		if (n > 98)
		{
			for(i = n; i 
		printf("%d", i);
		putchar(',');
		i++;
	}
		putchar('\n');
}
